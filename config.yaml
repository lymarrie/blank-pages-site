buildConfiguration:
  buildCommand: npm run build
  installDependenciesStep:
    command: npm install
    requiredFiles:
      - package.json
      - package-lock.json
      - .npmrc
livePreviewConfiguration:
  setupCommand: ":"

staticRoutes:
  - from: /index.html
    to: https://dynamic-routes-test-luc.pgsdemo.com/us.html
    status: 301

# dynamicRoutes:
#   - from: /index.html
#     to: /us.html
#     status: 302